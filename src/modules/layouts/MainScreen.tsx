import type { NextPage } from "next/types"
import * as React from "react"

interface MainScreenProps {}

const MainScreen: NextPage<MainScreenProps> = () => {
  return <div className="major-screen"></div>
}

export default MainScreen
